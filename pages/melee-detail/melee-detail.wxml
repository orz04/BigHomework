<view class="container">
  <view class="weapon-card panel">
    <view class="weapon-title">{{weapon.title}}</view>
    <image class="weapon-image" src="{{weapon.image}}" mode="aspectFit"></image>
  </view>

  <view wx:if="{{weapon.source}}" class="source-card panel">
    <view class="card-title">{{weapon.source.type}}</view>
    <view class="source-content">
      <block wx:if="{{weapon.source.description}}">
        <rich-text class="source-description" nodes="{{weapon.source.description}}"></rich-text>
      </block>
      <block wx:elif="{{weapon.source.dropList}}">
        <view wx:for="{{weapon.source.dropList}}" wx:key="enemy" class="source-item">
          <text class="source-label">{{item.enemy}}：</text>
          <text class="source-rate source-rate-{{item.rate === '常见' ? 'green' : item.rate === '罕见' ? 'yellow' : 'red'}}">{{item.rate}}</text>
        </view>
      </block>
      <block wx:else>
        <view class="source-item">
          <text class="source-label">{{weapon.source.type}}：</text>
          <text class="source-value">{{weapon.source.location}}</text>
          <text class="source-rate source-rate-{{weapon.source.rateColor}}">{{weapon.source.rate}}</text>
        </view>
      </block>
    </view>
  </view>

  <view wx:if="{{weapon.forging}}" class="forging-card panel">
    <view class="card-title">{{weapon.labels.forging}}</view>
    <view class="forging-row">
      <view class="forging-materials">
        <block wx:for="{{weapon.forging.materials}}" wx:key="index">
          <view class="forging-material">
            <view wx:if="{{item.icon}}" class="material-content">
              <image class="material-icon" src="{{item.icon}}" mode="aspectFit"></image>
              <text class="material-amount">{{item.amount}}</text>
            </view>
            <view wx:else class="material-content material-empty">
              <text class="material-na">{{item.amount}}</text>
            </view>
          </view>
        </block>
      </view>
      <view class="forging-info">
        <view class="forging-time">用时: {{weapon.forging.time}}</view>
        <view class="forging-speed">加速: <image class="speed-icon" src="https://636c-cloud1-d5gj26hlf342afe88-1433985605.tcb.qcloud.la/images/resourceimg/Platinum.png?sign=d7c751289a29089233f327a45b1b9d43&t=1779161854" mode="aspectFit"></image> {{weapon.forging.speed}}</view>
      </view>
    </view>
    <view class="forging-price-row">
      <block wx:for="{{weapon.forging.prices}}" wx:key="type">
        <view class="forging-price-cell">
          <text>{{item.type}}价格：</text>
          <image wx:if="{{item.currencyIcon}}" class="price-icon" src="{{item.currencyIcon}}" mode="aspectFit"></image>
          <text>{{item.amount}}</text>
        </view>
      </block>
    </view>
  </view>

  <view class="info-card panel">
    <view class="card-title">{{weapon.labels.basicInfo}}</view>
    <view class="info-table">
      <view class="table-row">
        <view class="table-cell-left">{{weapon.labels.type}}</view>
        <view class="table-cell-right">{{weapon.type}}</view>
      </view>
      <view class="table-row">
        <view class="table-cell-left">{{weapon.labels.slot}}</view>
        <view class="table-cell-right">{{weapon.slot}}</view>
      </view>
      <view class="table-row">
        <view class="table-cell-left">{{weapon.labels.mastery}}</view>
        <view class="table-cell-right">{{weapon.mastery}}</view>
      </view>
      <view class="table-row">
        <view class="table-cell-left">{{weapon.labels.range}}</view>
        <view class="table-cell-right">{{weapon.range}}</view>
      </view>
      <view wx:if="{{weapon.normalRadius}}" class="table-row">
        <view class="table-cell-left">{{weapon.labels.normalRadius}}</view>
        <view class="table-cell-right">{{weapon.normalRadius}}</view>
      </view>
      <view class="table-row">
        <view class="table-cell-left">{{weapon.labels.attackSpeed}}</view>
        <view class="table-cell-right">{{weapon.attackSpeed}}</view>
      </view>
      <view wx:if="{{weapon.blockAngle}}" class="table-row">
        <view class="table-cell-left">{{weapon.labels.blockAngle}}</view>
        <view class="table-cell-right">{{weapon.blockAngle}}</view>
      </view>
      <view wx:if="{{weapon.comboDuration}}" class="table-row">
        <view class="table-cell-left">{{weapon.labels.comboDuration}}</view>
        <view class="table-cell-right">{{weapon.comboDuration}}</view>
      </view>
      <view wx:if="{{weapon.damagePenetration}}" class="table-row">
        <view class="table-cell-left">{{weapon.labels.damagePenetration}}</view>
        <view class="table-cell-right">{{weapon.damagePenetration}}</view>
      </view>
      <view class="table-row">
        <view class="table-cell-left">{{weapon.labels.polarity}}</view>
        <view class="table-cell-right">
          <image class="polarity-icon" src="{{weapon.polarityIcon}}" mode="aspectFit"></image>
          <text>{{weapon.polarity}}</text>
        </view>
      </view>
      <view class="table-row">
        <view class="table-cell-left">{{weapon.labels.riftPolarity}}</view>
        <view class="table-cell-right">{{weapon.riftPolarity}}</view>
      </view>
    </view>
  </view>

  <view wx:if="{{weapon.attackData}}" class="detail-card panel">
    <view class="card-title">{{weapon.labels.attackInfo}}</view>

    <view class="attack-table">
      <view wx:if="{{weapon.attackData.totalDamage}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.totalDamage}}</view>
        <view class="attack-cell-right">{{weapon.attackData.totalDamage}}</view>
      </view>
      <view wx:if="{{weapon.attackData.attackSpeed}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.attackSpeed}}</view>
        <view class="attack-cell-right">{{weapon.attackData.attackSpeed}}</view>
      </view>
      <view wx:if="{{weapon.attackData.noise}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.noise}}</view>
        <view class="attack-cell-right">{{weapon.attackData.noise}}</view>
      </view>
      <view wx:if="{{weapon.attackData.statusChance}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.statusChance}}</view>
        <view class="attack-cell-right">{{weapon.attackData.statusChance}}</view>
      </view>
      <view wx:if="{{weapon.attackData.criticalChance}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.criticalChance}}</view>
        <view class="attack-cell-right">{{weapon.attackData.criticalChance}}</view>
      </view>
      <view wx:if="{{weapon.attackData.multiShot}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.multiShot}}</view>
        <view class="attack-cell-right">{{weapon.attackData.multiShot}}</view>
      </view>
      <view wx:if="{{weapon.attackData.criticalDamage}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.criticalDamage}}</view>
        <view class="attack-cell-right">{{weapon.attackData.criticalDamage}}</view>
      </view>
    </view>
  </view>

  <view wx:if="{{weapon.shootData}}" class="detail-card panel">
    <view class="card-title">{{weapon.labels.shootInfo}}</view>

    <view class="attack-table">
      <view wx:if="{{weapon.shootData.multiShot}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.multiShot}}</view>
        <view class="attack-cell-right">{{weapon.shootData.multiShot}}</view>
      </view>
      <view wx:if="{{weapon.shootData.totalDamage}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.totalDamage}}</view>
        <view class="attack-cell-right">{{weapon.shootData.totalDamage}}</view>
      </view>
      <view wx:if="{{weapon.shootData.chargeTime}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.chargeTime}}</view>
        <view class="attack-cell-right">{{weapon.shootData.chargeTime}}</view>
      </view>
      <view wx:if="{{weapon.shootData.triggerType}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.triggerType}}</view>
        <view class="attack-cell-right">{{weapon.shootData.triggerType}}</view>
      </view>
      <view wx:if="{{weapon.shootData.damageType}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.damageType}}</view>
        <view class="attack-cell-right">{{weapon.shootData.damageType}}</view>
      </view>
      <view wx:if="{{weapon.shootData.accuracy}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.accuracy}}</view>
        <view class="attack-cell-right">{{weapon.shootData.accuracy}}</view>
      </view>
      <view wx:if="{{weapon.shootData.penetration}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.penetration}}</view>
        <view class="attack-cell-right">{{weapon.shootData.penetration}}</view>
      </view>
      <view wx:if="{{weapon.shootData.spread}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.spread}}</view>
        <view class="attack-cell-right">{{weapon.shootData.spread}}</view>
      </view>
      <view wx:if="{{weapon.shootData.noise}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.noise}}</view>
        <view class="attack-cell-right">{{weapon.shootData.noise}}</view>
      </view>
      <view wx:if="{{weapon.shootData.damageRange}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.damageRange}}</view>
        <view class="attack-cell-right">{{weapon.shootData.damageRange}}</view>
      </view>
      <view wx:if="{{weapon.shootData.damageDecay}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.damageDecay}}</view>
        <view class="attack-cell-right"><rich-text nodes="{{weapon.shootData.damageDecay}}"></rich-text></view>
      </view>
      <view wx:if="{{weapon.shootData.statusChance}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.statusChance}}</view>
        <view class="attack-cell-right">{{weapon.shootData.statusChance}}</view>
      </view>
      <view wx:if="{{weapon.shootData.forcedStatus}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.forcedStatus}}</view>
        <view class="attack-cell-right">{{weapon.shootData.forcedStatus}}</view>
      </view>
      <view wx:if="{{weapon.shootData.criticalChance}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.criticalChance}}</view>
        <view class="attack-cell-right">{{weapon.shootData.criticalChance}}</view>
      </view>
      <view wx:if="{{weapon.shootData.criticalDamage}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.criticalDamage}}</view>
        <view class="attack-cell-right">{{weapon.shootData.criticalDamage}}</view>
      </view>
    </view>
  </view>

  <view wx:if="{{weapon.groundAttackData}}" class="detail-card panel">
    <view class="card-title">{{weapon.labels.groundAttackInfo}}</view>

    <view class="attack-table">
      <view wx:if="{{weapon.groundAttackData.totalDamage}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.totalDamage}}</view>
        <view class="attack-cell-right">{{weapon.groundAttackData.totalDamage}}</view>
      </view>
      <view wx:if="{{weapon.groundAttackData.attackSpeed}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.attackSpeed}}</view>
        <view class="attack-cell-right">{{weapon.groundAttackData.attackSpeed}}</view>
      </view>
      <view wx:if="{{weapon.groundAttackData.damageType}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.damageType}}</view>
        <view class="attack-cell-right">{{weapon.groundAttackData.damageType}}</view>
      </view>
      <view wx:if="{{weapon.groundAttackData.noise}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.noise}}</view>
        <view class="attack-cell-right">{{weapon.groundAttackData.noise}}</view>
      </view>
      <view wx:if="{{weapon.groundAttackData.damageRange}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.damageRange}}</view>
        <view class="attack-cell-right">{{weapon.groundAttackData.damageRange}}</view>
      </view>
      <view wx:if="{{weapon.groundAttackData.damageDecay}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.damageDecay}}</view>
        <view class="attack-cell-right"><rich-text nodes="{{weapon.groundAttackData.damageDecay}}"></rich-text></view>
      </view>
      <view wx:if="{{weapon.groundAttackData.statusChance}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.statusChance}}</view>
        <view class="attack-cell-right">{{weapon.groundAttackData.statusChance}}</view>
      </view>
      <view wx:if="{{weapon.groundAttackData.forcedStatus}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.forcedStatus}}</view>
        <view class="attack-cell-right">{{weapon.groundAttackData.forcedStatus}}</view>
      </view>
      <view wx:if="{{weapon.groundAttackData.criticalChance}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.criticalChance}}</view>
        <view class="attack-cell-right">{{weapon.groundAttackData.criticalChance}}</view>
      </view>
      <view wx:if="{{weapon.groundAttackData.criticalDamage}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.criticalDamage}}</view>
        <view class="attack-cell-right">{{weapon.groundAttackData.criticalDamage}}</view>
      </view>
    </view>
  </view>

  <view wx:if="{{weapon.heavyGroundAttackData}}" class="detail-card panel">
    <view class="card-title">{{weapon.labels.heavyGroundAttackInfo}}</view>

    <view class="attack-table">
      <view wx:if="{{weapon.heavyGroundAttackData.totalDamage}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.totalDamage}}</view>
        <view class="attack-cell-right">{{weapon.heavyGroundAttackData.totalDamage}}</view>
      </view>
      <view wx:if="{{weapon.heavyGroundAttackData.attackSpeed}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.attackSpeed}}</view>
        <view class="attack-cell-right">{{weapon.heavyGroundAttackData.attackSpeed}}</view>
      </view>
      <view wx:if="{{weapon.heavyGroundAttackData.damageType}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.damageType}}</view>
        <view class="attack-cell-right">{{weapon.heavyGroundAttackData.damageType}}</view>
      </view>
      <view wx:if="{{weapon.heavyGroundAttackData.noise}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.noise}}</view>
        <view class="attack-cell-right">{{weapon.heavyGroundAttackData.noise}}</view>
      </view>
      <view wx:if="{{weapon.heavyGroundAttackData.damageRange}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.damageRange}}</view>
        <view class="attack-cell-right">{{weapon.heavyGroundAttackData.damageRange}}</view>
      </view>
      <view wx:if="{{weapon.heavyGroundAttackData.damageDecay}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.damageDecay}}</view>
        <view class="attack-cell-right"><rich-text nodes="{{weapon.heavyGroundAttackData.damageDecay}}"></rich-text></view>
      </view>
      <view wx:if="{{weapon.heavyGroundAttackData.statusChance}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.statusChance}}</view>
        <view class="attack-cell-right">{{weapon.heavyGroundAttackData.statusChance}}</view>
      </view>
      <view wx:if="{{weapon.heavyGroundAttackData.forcedStatus}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.forcedStatus}}</view>
        <view class="attack-cell-right">{{weapon.heavyGroundAttackData.forcedStatus}}</view>
      </view>
      <view wx:if="{{weapon.heavyGroundAttackData.criticalChance}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.criticalChance}}</view>
        <view class="attack-cell-right">{{weapon.heavyGroundAttackData.criticalChance}}</view>
      </view>
      <view wx:if="{{weapon.heavyGroundAttackData.criticalDamage}}" class="attack-row">
        <view class="attack-cell-left">{{weapon.labels.criticalDamage}}</view>
        <view class="attack-cell-right">{{weapon.heavyGroundAttackData.criticalDamage}}</view>
      </view>
    </view>
  </view>
</view>
